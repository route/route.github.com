<p>obj =~ other → nil click to toggle source
Pattern Match—Overridden by descendants (notably Regexp and String) to provide meaningful pattern-match semantics.</p>
<div class="highlight"><pre class="highlight plaintext"><code>           static VALUE
</code></pre></div>
<p>rb<em>obj</em>match(VALUE obj1, VALUE obj2)
{
    return Qnil;
}</p>

<p>nil =~ // # =&gt; nil
nil !~ // # =&gt; true
//.match?</p>
